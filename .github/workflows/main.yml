name: First WorkFlow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: each "Hello World!"
      - name: Print goodbye
        run: echo "Goodbye"
